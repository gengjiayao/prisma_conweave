# Reward 函数完整审计（2026-05-12）

> 目的：审计当前 reward 公式每个分量的来源、网络语义、当前方向 vs 期望方向
> 触发：长训练 Test FCT 比 train 还差，验证 Agent 学反方向

---

## 当前 reward 公式

```cpp
r_inst = w_util  * T_port          // w_util  = 0.3
       + w_queue * qSmooth          // w_queue = 0.3
       + w_dup   * (1 - R_norm²)    // w_dup   = 0.4
       + 0      * (1 - R_qcn)       // 当前禁用
       - queueDerivPenalty          // 0.05 × derivNorm

r_level = α × r_inst + (1-α) × r_prev   // α = 0.7，EMA 平滑
```

Python Agent 收到的是 r_level（经过 EMA 平滑）。

---

## 分量逐项审计

### ① T_port（端口吞吐率，权重 0.3）

| 项目 | 内容 |
|------|------|
| **来源** | DRE 累加器：OnMacTx 每发包累加 dre_bytes，按 τ=1ms 指数衰减<br>`T_port = dre_bytes × 8 / (τ × bwBps)` |
| **网络语义** | 最近 ~τ 时间内该端口的真实利用率 = **端口繁忙程度** |
| **是否可靠** | ✓ 可靠（修复 obs normalize 后数值有效） |
| **当前方向** | T_port 越大 → reward 越大（正向贡献） |
| **CONGA/ConWeave 中同类指标的方向** | **DRE 是 cost**（越大越要避开） |
| **期望方向** | **应为负贡献**：T 越大表示端口越拥塞 → 越应避开 |
| **诊断结论** | ❌ **方向反了——这是 Agent 学反方向的根本原因** |

**论证**：
- CONGA 论文中 DRE 用作"本地拥塞代价"，选 DRE 最低的端口
- 我们用了同样的 DRE 累加器实现，但作为 reward 的正向项
- 等于让 Agent 学习"选 DRE 高的端口" = 与 CONGA 反向

### ② qSmooth（队列健康度，权重 0.3）

| 项目 | 内容 |
|------|------|
| **来源** | `qScore = 1/(1 + L/qRef)`（L 是 avgQueueBytes，EMA 平滑）<br>`qSmooth = (1-λ) × qSmooth + λ × qScore`，λ ≈ 0.2 |
| **网络语义** | 队列越满 qScore 越低；1 = 空队列，0 = 严重拥塞 |
| **是否可靠** | ✓ 设计正确 |
| **当前方向** | qSmooth 越大 → reward 越大（正向贡献） |
| **期望方向** | 一致（队列空 = 端口健康 = 好选择） |
| **诊断结论** | ✓ 方向正确 |

**但存在的问题**：
- 在 netload=20~50 的低/中负载下，qSmooth 几乎恒为 1.0（队列接近空）
- 端口间差异极小（实测 0.9990~0.9996）
- 实际贡献区分度接近零
- 只在高负载/拥塞场景下才有信号

### ③ R_norm（乱序率，权重 0.4）

| 项目 | 内容 |
|------|------|
| **来源** | 优先 per-flow dupAck EMA 平均；fallback 到 per-port dupAck EMA<br>由于 flow 映射 20ms TTL，实际几乎总是 fallback |
| **网络语义** | 该端口最近的乱序率（duplicate ACK 比例）<br>反映该路径触发了多少包乱序 |
| **是否可靠** | ✓ 可靠（per-port EMA 数据稳定） |
| **当前方向** | `(1 - R²)`，R 越大（乱序多）→ reward 越小 |
| **期望方向** | 一致（乱序少 = 好选择） |
| **诊断结论** | ✓ 方向正确 |

**额外观察**：
- 平方处理 `R²` 让小乱序时差异更小、大乱序时差异更大——合理
- 但 R_norm 是 EMA 平滑值，反映"过去一段"，对 Agent 即时反馈有滞后
- 实测 R_norm ≈ 0.53 比较稳定，端口间差异 0.01~0.03

### ④ queueDerivPenalty（队列变化率惩罚，权重 0.05）

| 项目 | 内容 |
|------|------|
| **来源** | OnMacTx 中算队列字节的变化率 EMA<br>`derivNorm = queueDerivEma / BDP`，clamp 到 [0,1]<br>`penalty = 0.05 × derivNorm`（仅当队列上升时） |
| **网络语义** | 队列正在上升的速率，反映"拥塞趋势" |
| **是否可靠** | ✓ 设计正确（PD 控制思想） |
| **当前方向** | 作为减项，队列上升时扣分 |
| **期望方向** | 一致（队列上升预警 → 避开） |
| **诊断结论** | ✓ 方向正确 |

**实际贡献**：权重 0.05 × derivNorm ∈ [0,1] → 实际惩罚 [0, 0.05]，影响很小。

### ⑤ R_qcn（QCN/NACK，权重 0）

| 项目 | 内容 |
|------|------|
| **来源** | `nack/(ack+nack)` 比例 |
| **网络语义** | QCN 触发频率，反映拥塞控制信号 |
| **权重** | `m_rewardWQcn = 0.0`（当前禁用） |
| **诊断结论** | 未启用，不影响 |

---

## 整体诊断：信号方向矩阵

| 分量 | 当前方向 | 期望方向 | 状态 |
|------|---------|---------|------|
| T_port (0.3) | **正贡献** | 负贡献（cost）| **❌ 反了** |
| qSmooth (0.3) | 正贡献 | 正贡献 | ✓ |
| (1-R²) (0.4) | 正贡献 | 正贡献 | ✓ |
| queueDerivPenalty (0.05) | 减项 | 减项 | ✓ |

**只有 T_port 的方向是反的，但它占 30% 权重，足以扭曲整体学习方向**。

---

## 修复方案

### 方案 A（最小改动，强烈推荐）：T_port 改为负贡献

```cpp
// 原：
double r_inst = w_util * T_port + ...

// 改为：
double r_inst = w_util * (1.0 - T_port) + ...
```

`(1 - T_port)` ∈ [0,1] 且"端口越闲值越大"，所有项语义统一"越大越好"。

### 方案 B（更激进）：去掉 T_port 项，权重转移

```cpp
// w_util = 0;  从0.3 直接置零
// w_queue = 0.4;  
// w_dup = 0.6;
```

但 qSmooth 在低负载下贡献接近 0，整个 reward 主要由 R_norm 推动，分辨率会下降。

### 方案 C（保留奖励吞吐的语义，但用对方法）

把 T_port 改造为"该 Agent 路径贡献的吞吐"而非"端口总吞吐"：
- 只统计 RL hold 过的 flowlet 字节
- 同时**只在该端口未拥塞时**给奖励（T_port < threshold 才奖励）
- 工程量大（~50 行修改），效果未必比方案 A 好

---

## 推荐路径

1. **第一步（今天就做）**：方案 A，改 1 行代码，跑短训验证
   - 验证标准：Agent 动作分布应该转向"低 T 端口"
   - 端口间 reward 差距和 SNR 应继续提升

2. **第二步（如果方案 A 有效但仍未超过 ECMP）**：考虑是 qSmooth 在低负载下无贡献的问题
   - 提升 netload 到 70+，让 qSmooth 真正激活
   - 或者把 qSmooth 改成更敏感的形式（如对数缩放）

3. **第三步（如果还不行）**：才考虑 hold-only 改造或 per-flowlet end-to-end reward

---

## 关键认知

**过去 5 个月所有"reward 改不好"的痛苦**，根本原因**不是工程 bug**（虽然踩了 obs normalize、T 时间尺度等多个坑），而是**对 CONGA DRE 设计哲学的根本性误用**：

- CONGA：DRE 是 cost，选最小
- 我们：DRE 改名 T_port，当 reward 正项用，选最大
- 等于把 CONGA 的智慧反过来用了

这个错误足够隐蔽——因为：
1. T_port 用 DRE 实现是对的（用累加器避免时间尺度错位）
2. qSmooth、R_norm 方向都是对的
3. 公式形式上看不出问题（所有项都是 [0,1] 正值加权和）

只有当 Agent 学反方向 + 系统审计每个分量的物理语义后，才暴露出来。

---

## 副产品发现：qSmooth 在低负载下是"沉默贡献"

即使修复 T_port 方向后，reward 的 0.3 权重 qSmooth 在 netload=50 下几乎恒为 1.0，**实际只用了 0.4 权重的 R_norm 在做区分**。这意味着：

- 真正驱动 Agent 学习的信号只有 40%（R_norm 项）
- 高负载下 qSmooth 才会激活
- 这解释了为什么之前"低负载下 RL 学不到东西"——根本没有足够多的可学信号
