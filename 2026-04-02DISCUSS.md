# 阶段性讨论与结论 2026-04-02

---

## 一、当前实验结论

### 1.1 netload=50 长训练结果（run 375960720，训练时长 1d9h）

- Reward 稳定在 0.83-0.85，无上升趋势
- HuberLoss 在 0.00x 级别微幅抖动
- 动作分布 early vs late 均近似均匀（9%~17%）
- **Agent 未学到有效策略**

### 1.2 根因诊断

Per-port reward 统计：

| 端口 | avg_reward | avg_R_norm | reward std |
|------|-----------|-----------|-----------|
| 最好 (port_23) | 0.8421 | 0.5065 | 0.0465 |
| 最差 (port_17) | 0.8356 | 0.5180 | 0.0454 |
| **端口间差距** | **0.0065** | **0.0115** | - |

**信噪比 = 端口间差距 / 端口内噪声 = 0.007 / 0.045 = 0.15**

DQN 无法从 SNR=0.15 的信号中区分动作好坏。Q 网络将所有动作的 Q 值拟合到均值 ~0.84，argmax 等效随机。

### 1.3 物理原因

8 端口对称 Leaf-Spine 拓扑 + netload=50（每端口约 6.25%）下：
- 所有 Spine 端口的拥塞/乱序水平天然趋同
- 即使 netload=50，负载在 8 条对称链路上均匀分摊，远未到产生拥塞差异化的程度
- **RL 没有优化空间，不是算法问题，是实验条件问题**

---

## 二、Reward 设计状态

### 已完成

| 分量 | 状态 | 说明 |
|------|------|------|
| T（吞吐） | 权重临时置零 | flow EMA 生命周期与 flowlet 调度时间尺度错位，恒为 0 |
| qSmooth（队列） | 正常但信息量低 | 低/中负载下恒为 1.0，高负载下会激活 |
| R_norm（乱序） | per-port，逻辑正确 | 低/中负载下端口间差异极小 |
| queueDerivPenalty | 正常 | 队列变化率惩罚 |

### 待完成

| 任务 | 方案 | 优先级 |
|------|------|--------|
| T 重设计 | per-port 真实吞吐率：accTxBytes 增量 / 时间窗口 / 带宽 | 高 |
| reward EMA 平滑评估 | 当前 r = 0.7*r_inst + 0.3*r_prev，考虑去掉或减小惯性 | 中 |

### 训练逻辑环路评估

obs -> action -> reward -> replay buffer -> train -> Q update 的全链路已验证通过。**环路无结构性问题**，瓶颈在 reward 信噪比（由实验条件决定）。

---

## 三、ECMP 的弱点与 RL 的机会窗口

### ECMP 的核心缺陷

ECMP 使用静态哈希将流映射到端口，存在 hash collision 问题：
- 多条流被哈希到同一端口 -> 局部过载，其他端口闲置
- 碰撞概率随并发流数量超线性增长（birthday problem）
- **流的路径终生不变，即使目标端口已拥塞也不调整**

### 碰撞严重度与负载的关系

- netload=20~50：并发流少，碰撞概率低，ECMP 表现良好
- **netload=70+：碰撞成为主要瓶颈，某些端口会出现 20%+ 负载而其他仅 5%**
- netload=90+：碰撞几乎不可避免，ECMP 的 FCT 急剧恶化

### RL 超越 ECMP 的条件

RL 的优势建立在"不同端口之间存在可感知的负载差异"上：
1. RL 能感知实时拥塞（CONGA 观测特征），发现过载端口后主动分流
2. RL 以 flowlet 粒度动态调整，而非像 ECMP 一锤定音
3. 高负载下 ECMP 碰撞产生的端口间负载差异，正好是 RL 观测特征的有效信号

**推荐实验条件**：netload=70~80，此时 ECMP 碰撞严重，端口间拥塞差异大，RL 的观测特征信噪比高。

### AI workload 场景的额外优势

AI 训练负载（AllReduce）的特征对 ECMP 特别不利：
- **极大流为主**（MB~GB 级梯度同步），大流碰撞的持续影响远大于小流
- **突发性强**（同步点所有 worker 同时发送），瞬时负载可达线速
- **周期性通信模式**，RL 可以学到规律并提前优化

这是一个有力的论文叙事角度：传统 CDF 下 ECMP 碰撞影响有限，但 AI workload 放大了 ECMP 的弱点，RL 的动态调度优势更明显。

---

## 四、对 CONGA 和 ConWeave 的竞争力分析

### 4.1 vs CONGA

**CONGA 的工作方式**：
- 每个 Leaf 维护到每个目的 Leaf 的 per-port 拥塞度（本地 DRE + 远端反馈 piggyback）
- 新 flowlet 选择拥塞度最低的端口
- 是确定性贪心算法：每次选当前最优，不考虑未来

**RL 的潜在优势**：
- CONGA 是 myopic（只看当前最优），RL 有 gamma 折扣可以前瞻。但在微秒级决策下，前瞻优势不大
- CONGA 使用固定的拥塞度函数（DRE），RL 可以学到更灵活的非线性映射
- **实际差距可能很小**：CONGA 本身就是一个很强的 baseline，在对称拓扑下接近最优

**RL 超越 CONGA 的可能场景**：
- 非对称故障（某条链路降速），CONGA 的反馈延迟可能导致短暂的次优决策，RL 可以更快反应
- 流量模式有可学习的规律性（如 AI workload 的周期性），CONGA 无法利用但 RL 可以
- **坦率说，在常规对称场景下超越 CONGA 的难度很大**

### 4.2 vs ConWeave

**ConWeave 的工作方式**：
- 在 CONGA 基础上增加了 VOQ（Virtual Output Queue）机制解决乱序问题
- 检测到乱序风险时主动暂停 flowlet 切换，在 egress 端用 VOQ 重排序
- 是目前学术界数据中心路由的 SOTA

**RL 的潜在优势**：
- ConWeave 的 VOQ 机制是被动的（先乱序再修复），RL 可以主动避免乱序（预测哪个端口会导致乱序并避开）
- ConWeave 的参数（VOQ flush time、path pause time）是手调的，RL 可以自适应

**现实评估**：超越 ConWeave 在 2 个月内**非常困难**。ConWeave 是专门为解决数据中心路由乱序设计的系统级方案，经过了大量工程优化。RL 在算法层面可能找到某些场景下的优势，但在系统成熟度上差距很大。

### 4.3 务实定位

| 对手 | 超越难度 | 可能的突破场景 |
|------|---------|-------------|
| ECMP | **可行** | netload>=70，AI workload，链路故障 |
| CONGA | **困难但有机会** | 非对称故障，周期性流量 |
| ConWeave | **2个月内不现实** | 极端突发场景可能有局部优势 |

**建议的论文定位**：RL vs ECMP 为主线，vs CONGA 为加分项。不必追求全面超越 ConWeave，而是展示 RL 方法在特定场景下的潜力和可解释性优势。

---

## 五、项目亮点与差距评估

### 已完成的亮点

1. **系统集成**：打通了 PRISMA（MARL 框架）与 ConWeave（NS3 数据中心仿真器）的全链路通信。这本身是一项不小的工程工作——两个独立系统通过 ZMQ + Protobuf 逐步交互，观测/动作/奖励全链路闭环
2. **Reward 缺陷发现与修复**：通过 reward 分量拆解发现 T 指标的时间尺度错位问题，这种定量诊断方法本身有方法论价值
3. **CONGA 特征观测接入**：将 NS3 中维护的真实网络状态指标（5 维 CONGA 特征）注入 RL 观测空间
4. **决策粒度正确对齐**：flowlet 级决策 + 驻留锁 + 正确的覆盖率理解

### 离真正发挥效果的差距

| 差距 | 描述 | 工作量估计 |
|------|------|-----------|
| **实验条件不足** | 需要 netload>=70 才能制造端口间差异，让 RL 有信号可学 | 1~2 天（调参+跑实验） |
| **T 指标缺失** | reward 与 FCT 的桥梁断裂，需要用 per-port 吞吐率替代 | 1 天（改 ComputeReward） |
| **ECMP baseline 缺失** | 没有对比数据，无法量化优势 | 1 天（跑 baseline） |
| **FCT 评估未对接** | 训练用 reward，评估应用 FCT slowdown | 2 天（解析 fct 输出+绘图） |
| **高负载下验证 Agent 能学习** | 核心里程碑：动作分布偏离均匀 + R_norm 下降 | 3~5 天 |
| **多条件泛化** | 不同负载、不同 CDF、链路故障 | 1~2 周 |

### 两个月够吗

**够，但需要聚焦**。

关键路径：高负载实验(1周) -> T重设计+验证Agent学习(1周) -> ECMP对比(1周) -> 多条件实验(2周) -> 论文撰写(2周) = 约 7 周

**风险点**：如果 netload=70~80 下 Agent 仍然不学习，需要重新审视 DQN 是否适合这个问题（可能需要换成 PPO 或 SAC 等对微弱信号更敏感的算法）。这是最大的技术风险，建议在 4 月中旬前确认。

---

## 六、后续待做事项（按优先级）

### 本周（4月2日-4月4日）

- [ ] 跑 ECMP baseline：netload=50 和 netload=70，记录 FCT
- [ ] 跑 RL test（当前模型）：同样条件，对比 FCT
- [ ] 量化 RL vs ECMP 的差距

### 下周（4月7日-4月11日）

- [ ] 实现 T 重设计（per-port 真实吞吐率）
- [ ] netload=70~80 长训练，验证 Agent 能否学到策略
- [ ] 如果动作分布偏离均匀 -> 进入对比实验阶段
- [ ] 如果仍不学习 -> 考虑减小 reward EMA 或换 RL 算法

### 4月中下旬

- [ ] 多负载 FCT 对比（netload=50/70/80/90）
- [ ] AI workload CDF 实验（如果 ConWeave 支持 incast 模式）
- [ ] 链路故障注入实验

### 5月

- [ ] 多拓扑规模验证（可选）
- [ ] 实验数据整理 + 对比图表
- [ ] 论文/报告撰写


---

## 附录：100Gbps 实验命令集

> 前置条件：已将 `config/leaf_spine_128_100G_OS2.txt` 中链路改回 100Gbps，`run.py` 中 BDP 改回 500000

### ECMP Baseline（直接用 ConWeave run.py，不走 PRISMA）

ECMP/CONGA 等非 RL 算法不需要 Python Agent（NS3 只在 lb_mode=7/rl 时创建 ZMQ），因此必须直接从 ConWeave 目录启动：

```bash
cd /workspace/PRISMA_conweave/conweave-ns3-main

# ECMP netload=50
# 注意：ECMP baseline 也从 PRISMA main.py 启动（非 run.py）
# python3 run.py 直接启动会因缺少 overlay 配置文件而失败
cd /workspace/PRISMA_conweave/prisma
python3 main.py --train=1 --lb=fecmp --cc dctcp --pfc 0 --irn 1 \
  --simul_time 2 --buffer 50 --netload 50 --bw 100 \
  --topo leaf_spine_128_100G_OS2 --cdf AliStorage2019

# ECMP netload=70
# 注意：ECMP baseline 也从 PRISMA main.py 启动（非 run.py）
# python3 run.py 直接启动会因缺少 overlay 配置文件而失败
cd /workspace/PRISMA_conweave/prisma
python3 main.py --train=1 --lb=fecmp --cc dctcp --pfc 0 --irn 1 \
  --simul_time 2 --buffer 50 --netload 70 --bw 100 \
  --topo leaf_spine_128_100G_OS2 --cdf AliStorage2019

# CONGA netload=50（可选，作为更强的 baseline）
python3 run.py --cc dctcp --lb conga --pfc 0 --irn 1 \
  --simul_time 2 --buffer 50 --netload 50 --bw 100 \
  --topo leaf_spine_128_100G_OS2 --cdf AliStorage2019
```

输出在 `mix/output/[ID]/`，看 `*_out_fct_summary.txt` 获取 FCT 数据。100G 下 simul_time=2 预计几分钟完成。

### RL 训练（通过 PRISMA main.py 启动）

```bash
cd /workspace/PRISMA_conweave/prisma

# RL 训练 netload=50, 100Gbps
python3 main.py --train=1 --netload=50 --buffer=50 --simul_time=2 \
  --exploration_schedule_timesteps=30000 --exploration_final_eps=0.05 \
  --replay_buffer_max_size=100000 --pfc=0 --irn=1 --cc=dctcp --bw=100 \
  --session_name=rl_100g_net50 --gamma=0.9

# RL 训练 netload=70, 100Gbps
python3 main.py --train=1 --netload=70 --buffer=50 --simul_time=2 \
  --exploration_schedule_timesteps=30000 --exploration_final_eps=0.05 \
  --replay_buffer_max_size=100000 --pfc=0 --irn=1 --cc=dctcp --bw=100 \
  --session_name=rl_100g_net70 --gamma=0.9
```

### RL Test（加载训练好的模型，无探索噪声）

```bash
cd /workspace/PRISMA_conweave/prisma

# test 模式：epsilon=0，100% 使用 Q 网络 argmax
python3 main.py --train=0 --netload=50 --buffer=50 --simul_time=2 \
  --pfc=0 --irn=1 --cc=dctcp --bw=100 \
  --load_path=examples/abilene/results/saved_models/rl_100g_net50/final \
  --session_name=rl_100g_net50
```

### 性能评估方式

**应使用 test 模式（--train=0）评估 RL 性能**，而非 train 模式：

| 对比项 | train 模式 | test 模式 |
|--------|-----------|----------|
| epsilon | 1.0 -> 0.05 衰减中 | 0（纯策略） |
| 动作来源 | 混合随机+策略 | 100% Q 网络 argmax |
| 数据代表性 | 包含大量随机探索噪声 | 反映模型真实性能 |
| 输出 | reward/loss 曲线 | injected/delivered/dropped/e2e_delay |
| 对标方式 | 看学习趋势 | 和 ECMP FCT 直接对比 |

**对比流程**：
1. 跑 ECMP baseline -> 拿 `*_out_fct_summary.txt`
2. RL train -> 保存模型
3. RL test（同参数，load 模型）-> 拿 `*_out_fct_summary.txt`
4. 对比两者的 FCT slowdown（small/large flow 分开看）

### 推荐执行顺序

1. 先跑 ECMP netload=50（验证 100G 环境 + 拿 baseline FCT）
2. 再跑 ECMP netload=70（看高负载下 ECMP 的 FCT 恶化程度）
3. 跑 RL train netload=50 或 70（看 100G 下训练速度和学习信号）
4. RL test 对比


---

## 附录修正：100Gbps 实验命令集（04-02 更新）

> 所有命令统一从 PRISMA main.py 启动，不直接调用 ConWeave run.py

### 100Gbps 恢复相关改动

| 文件 | 改动 |
|------|------|
| `config/leaf_spine_128_100G_OS2.txt` | 1Gbps -> 100Gbps |
| `run.py` topo2bdp | 5000 -> 104000 |
| `scratch/network-load-balance.cc` topo2bdpMap | 5000 -> 104000 |

BDP = 100Gbps * RTT(8320ns) / 8 = 104000 bytes

### ECMP Baseline

```bash
cd /workspace/PRISMA_conweave/prisma

# ECMP netload=50, 100Gbps
python3 main.py --train=1 --netload=50 --buffer=50 --simul_time=2 \
  --pfc=0 --irn=1 --cc=dctcp --bw=100 \
  --lb=fecmp --session_name=ecmp_100g_net50

# ECMP netload=70, 100Gbps
python3 main.py --train=1 --netload=70 --buffer=50 --simul_time=2 \
  --pfc=0 --irn=1 --cc=dctcp --bw=100 \
  --lb=fecmp --session_name=ecmp_100g_net70
```

### RL 训练

```bash
cd /workspace/PRISMA_conweave/prisma

# RL netload=50, 100Gbps
python3 main.py --train=1 --netload=50 --buffer=50 --simul_time=2 \
  --exploration_schedule_timesteps=30000 --exploration_final_eps=0.05 \
  --replay_buffer_max_size=100000 --pfc=0 --irn=1 --cc=dctcp --bw=100 \
  --session_name=rl_100g_net50 --gamma=0.9

# RL netload=70, 100Gbps
python3 main.py --train=1 --netload=70 --buffer=50 --simul_time=2 \
  --exploration_schedule_timesteps=30000 --exploration_final_eps=0.05 \
  --replay_buffer_max_size=100000 --pfc=0 --irn=1 --cc=dctcp --bw=100 \
  --session_name=rl_100g_net70 --gamma=0.9
```

### RL Test（加载模型，无探索）

```bash
cd /workspace/PRISMA_conweave/prisma

python3 main.py --train=0 --netload=50 --buffer=50 --simul_time=2 \
  --pfc=0 --irn=1 --cc=dctcp --bw=100 \
  --load_path=examples/abilene/results/saved_models/rl_100g_net50/final \
  --session_name=rl_100g_net50
```
