# Session Handoff Document — 2026-05-19
> 上一份 handoff：`HANDOFF_2026-05-18.md`（项目历史 + 14 bug 归档）
> 本次主题：**对抗场景设计 + 深度环路 audit + 5 个隐藏 bug 一并修复**

---

## 一、本次会话目标 vs 实际产出

| 目标 | 状态 |
|---|---|
| 决策"刁难 ECMP" 还是"重构 RL" | ✅ 决定走对抗场景（异速 spine） |
| 设计异速 topology 并落地 | ✅ `leaf_spine_128_100G_asym_OS2.txt`（4 慢 spine） |
| 给训练命令 | ✅ 见下面"训练命令" section |
| 深度 audit RL 环路找隐藏 bug | ✅ 发现 5 个 + 3 个结构性弱点 |
| 修复发现的隐藏 bug | ✅ 5 个全修，已落 server |
| 自动批准 watcher 部署 | ✅ `~/Code/tools/cc-auto/` + `claude-approve` tmux 后台 |

---

## 二、Server 代码改动总览（**重编必须**）

### C++ 改动（`./waf build` 才生效）

| 文件 | 改动 | Backup |
|---|---|---|
| `src/opengym/model/conweave-obs-manager.cc:485-498` | **Bug #1**：ResolveLinkBandwidthBps 改为查询真实 DataRate | `/tmp/conweave-obs-manager.cc.bak_asym` |
| `src/opengym/model/conweave-obs-manager.cc:400-420` | **Bug #2**：queueDerivEma 用 OLD lastQueueSampleSec 算 dt | `/tmp/obs-manager.cc.bak_audit` |
| `src/opengym/model/conweave-obs-manager.cc:222-243` | **Bug #5**：CONGA features 改用固定 scale=5000 | 同上 |
| `scratch/network-load-balance.cc:1632` | 新增 `leaf_spine_128_100G_asym_OS2` 到 topo2bdpMap | （无） |

### Python 改动（kill main.py 重启即可）

| 文件 | 改动 | Backup |
|---|---|---|
| `prisma/source/forwarder.py:549` | **Bug #3a**：replay_buffer.add 传 ecmp_action | `/tmp/forwarder.py.bak_audit` |
| `prisma/source/trainer.py:87` | **Bug #3b**：IL phase 末尾 `return` 改为只在 IL 真跑了时才 return | `/tmp/trainer.py.bak_audit` |
| `prisma/source/models.py DQN_buffer_model` | **Bug #6**：size=1 的 split 跳过 LayerNorm | `/tmp/models.py.bak_audit` |

### Topology 改动

| 文件 | 改动 | Backup |
|---|---|---|
| `conweave-ns3-main/config/leaf_spine_128_100G_asym_OS2.txt` | 新建：4 个慢 spine（140-143 是 500Mbps） | (新文件) |
| `conweave-ns3-main/run.py` | topo2bdp dict 加新条目 | `run.py.bak` |

---

## 三、5 个隐藏 bug 一句话总结

详见同目录 `AUDIT_HIDDEN_BUGS_2026-05-19.md`。

1. **#1 ResolveLinkBandwidthBps 硬编码 1Gbps** → 异速下 T_port 全错
2. **#2 queueDerivEma 计算顺序 → dt 永远 0** → obs 8 维死信号 + 队列趋势 reward 永远 0
3. **#3 IL 训练完全断路** → Design B (IL warmup) 前 N 步 Q-network 完全没更新，解释了"IL 保底未兑现"
4. **#5 CONGA feature scaling 用 per-port BDP** → bug #1 修复后引入，异速下慢链反而看起来空闲
5. **#6 LayerNorm(axis=1) 在 size=1 维输出永远 0** → lastAction 特征死了，**所有历史 RL 训练都受此影响**（解释了 8 端口选择概率 11-14% 这么均匀）

---

## 四、对抗场景（异速 spine）设计

**拓扑修改**：8 spines 中后 4 个（spine 140-143）下行链路从 1Gbps 改为 500Mbps：
- 4×1Gbps + 4×500Mbps = 总容量 6Gbps（对称版 8Gbps）
- 32 条 leaf-spine 链路修改（8 leaves × 4 慢 spines）
- m_overlayNeighbors 顺序保持 [136, 137, ..., 143] → **action_id 0-3 = 快链，action_id 4-7 = 慢链**

**ECMP 在该场景下的结构性失败**：
- 在 netload=70 下流量约 5.6 Gbps
- ECMP 均匀分发 → 每端口 0.7 Gbps
- 快链 70% 利用率 OK；**慢链 140% 利用率 → 饱和，长尾爆**
- 预测：异速 ECMP <1BDP FCT ≈ 5-10（vs 对称 ECMP=3.089）

**RL 在该场景下的预期**：
- 修完 bug 后 RL 能从 T_port/queueDeriv/lastAction 多个维度看出"慢链不好"
- 理论最优：容量比例分配，快链承载 ~67% 流量
- 预测：异速 RL FCT ≈ 3.5-5（接近对称 ECMP）
- **gap = 5-50%，RL 反超概率 60-70%**

---

## 五、训练命令（即将执行）

### Step 1 — 重编 NS-3（必须）

```bash
docker run -it --rm --name conweave-prisma-container -u 1001:1001 \
  -v ~/Cyq_RL/PRISMA_conweave:/workspace/PRISMA_conweave conweave-prisma bash

# in docker:
cd /workspace/PRISMA_conweave/conweave-ns3-main
./waf build 2>&1 | tail -30
```

### Step 2 — ECMP baseline（异速）

```bash
cd /workspace/PRISMA_conweave/prisma
python3 main.py --train=0 --netload=70 --buffer=50 --simul_time=2 \
    --topo=leaf_spine_128_100G_asym_OS2 --lb=fecmp \
    --pfc=0 --irn=1 --cc=dctcp \
    --session_name=asym_ecmp_load70
```

### Step 3 — RL 训练（异速，纯 RL）

```bash
cd /workspace/PRISMA_conweave/prisma
python3 main.py --train=1 --netload=70 --buffer=50 --simul_time=1 \
    --topo=leaf_spine_128_100G_asym_OS2 \
    --exploration_schedule_timesteps=30000 --exploration_final_eps=0.05 \
    --replay_buffer_max_size=150000 --pfc=0 --irn=1 --cc=dctcp \
    --session_name=asym_rl_load70_clean --gamma=0.9
```

**关键参数说明**：
- `--lb` 默认 'rl'，不传即可
- `--il_phase_steps` 不传 → 默认 0 → 纯 DQN，无 IL warmup
- `--simul_time=1` → 约 50K transitions per leaf；如果训练不充分可拉到 2
- `--exploration_schedule_timesteps=30000` → 探索率 1.0 在 30000 步衰减到 final_eps

---

## 六、对 IL warmup 的最终判断

**修完 Bug #3 后 IL 真正能跑了**，但：
- 对称场景：IL 把 Q-network 预训练成 ECMP-equivalent，DQN 在此基础上微调，理论"只涨不跌"——**保底承诺成立**
- **异速场景：ECMP 本身就是错答案，用它做 teacher 等于教 RL 学一个反向最优政策，DQN 还得 unlearn → 比从随机初始化还慢**
- 所以异速场景**不要开 IL**，等以后回到对称做对比实验时再启用

更好的"保底"思路（**留给以后做**）：训练中定期 eval + 保留 FCT 最低 checkpoint。

---

## 七、Watcher 自动批准（永久工具）

部署位置：`~/Code/tools/cc-auto/`

启动命令（在本机另一个 tmux/终端跑）：
```bash
tmux new -d -s claude-approve "$HOME/Code/tools/cc-auto/cc-approve.sh -s RL_claude -w 0 --fast"
```

监控/停止：
```bash
tmux attach -t claude-approve    # 看监控状态
tmux kill-session -t claude-approve   # 停掉
```

危险命令黑名单在 `cc-approve.sh:44-53`，已覆盖 `rm -rf` / `sudo` / `git push --force` / `chmod 777` / `DROP TABLE` / `reboot` / `shutdown`。

---

## 八、下一个 session 接力指南

### 优先做
1. **跑训练**（用户 offline 跑，提供 output ID 给下一 session）
   - asym ECMP baseline
   - asym RL clean training
2. **看结果**
   - 异速 ECMP 是否 ≥ 5（预期）
   - 异速 RL 是否 < ECMP（核心 claim）
   - 看 reward_ewma 曲线是否收敛
   - 看 8 端口选择概率（修完 Bug #6 后应该不再均匀 11-14%，快链 spine 应该选择频率更高）

### 如果 RL > ECMP
- 写入论文素材
- 测多个 netload 点（50%, 60%, 70%, 80%）画曲线
- 测不同异速比例（1/3 vs 1/2 vs 1/1 slow:fast）

### 如果 RL ≈ ECMP 或更差
- 检查训练曲线（reward 是否收敛？epsilon 是否够低？）
- 加大 simul_time 到 2s
- 看 reward CSV (`mix/output/<ID>/reward_breakdown_sw*.csv`) 诊断 per-port reward 信号
- 可能需要 checkpoint-eval 机制找训练中最好的那个

### 留给以后的小事
- 弱点 A：`RlTimeoutFallback` 加 OnRlTimeout 实现（安全网）
- 弱点 B：m_egressIfs vs nexthops 顺序 explicit 检查（防御性）
- 弱点 C：最终 Dense 层 `activation='elu'` 改 linear

---

## 九、文档清单（本次新增 + 沿用）

新增（本目录）：
- `AUDIT_HIDDEN_BUGS_2026-05-19.md` — 5 个 bug 详细复现 + 修复 patch（已更新为"已修"状态）
- `HANDOFF_2026-05-19.md` — 本文件

沿用前期：
- `HANDOFF_2026-05-18.md` — 项目历史 + 14 bug 归档
- `DESIGN_A_PER_FLOWKEY_ROUTING_2026-05-14.md`
- `DESIGN_BC_FEASIBILITY_2026-05-14.md`
- `RL_BUGS_AND_CEILINGS_2026-05-13.md`
- `STAGE_SUMMARY_2026-05-12.md`

---

## 十、关键资源位置（速查）

### Server (simAI-dir)
- 代码：`/home/cuiyiqin/Cyq_RL/PRISMA_conweave/`
- C++ 改动备份：`/tmp/conweave-obs-manager.cc.bak_*`、`/tmp/*.bak_audit`
- Python 改动备份：`/tmp/forwarder.py.bak_audit` 等
- 训练输出：`conweave-ns3-main/mix/output/<ID>/`
- 历史 .history：`conweave-ns3-main/mix/.history`

### Local Mac
- 代码：`~/Code/ICT/RL/PRISMA_conweave/`（GitHub 中转）
- 工具：`~/Code/tools/cc-auto/`（自动批准 watcher）
- 桌面 SOP：`~/Desktop/GIT_PUSH_SOP.md`

### GitHub
- Repo：`https://github.com/cuiyiqin/prisma-conweave-rl.git`
- 当前未推送本次改动（**待下次 session 决定是否打新 tag 如 v0.9-bug-audit-fixes**）

---

**Handoff 结束。下一 session 等用户提供 asym ECMP + asym RL 的 output ID 后展开诊断。**
